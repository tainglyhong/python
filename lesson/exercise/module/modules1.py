"""
Modules in Python are a files that contain Python code.
which can include functions, classes, and variables.
"""

import math  # importing the math module
import math as m
# from math import sqrt, fact


print(math.sqrt(16))  # using the sqrt function from the math module
# print(sqrt(16))  
# print(fact(5)) 
print(math.log2(8))

print(math.pi)  # using the pi constant from the math module

print(math.factorial(5))  # using the factorial function from the math module

print(math.pow(2, 3))  # using the pow function from the math module

print(math.sin(math.pi / 2))  # using the sin function from the math module

print(math.cos(0))  # using the cos function from the math module

print(m.sin(0))
