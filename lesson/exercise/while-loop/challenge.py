"""
1.  # Use a while loop to repeat rolling two six-sided dice 10 times.
    # On each loop iteration, generate two random dice values,
    # calculate the total, print the dice sum,
    # and increment the loop counter until 10 rolls are completed.
    
2.  # Create a variable to keep track of the total of all dice rolls.
    # During each loop iteration, add the current dice_sum
    # to the running total.
    # After 10 rolls, calculate and print the average
    # by dividing the total sum by the number of rolls.
    
3.  # Use input() to ask the user how many times
    # they want to repeat the dice rolling experiment.
    # Run the while loop based on the user's input,
    # calculate the running total of all rolls,
    # and print the average using the total number of repetitions.
"""

# import random

# # Roll two six-sided dice.
# first_die = random.randint(1, 6)
# second_die = random.randint(1, 6)

# dice_sum = first_die + second_die
# print("You rolled a " + str(dice_sum) + "!")
